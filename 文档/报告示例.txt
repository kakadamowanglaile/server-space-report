服务器空间去哪了 0.1.1
这是人为构造的展示示例，不是真实机器的测量结果。

[完成] 分区空间
检查范围：/
总字节：40.00 GiB
已用字节：35.00 GiB
可用字节：3.00 GiB
说明：空闲与普通用户可用空间可能不同，部分空间由文件系统保留。

[完成] 大目录
检查范围：/var
目录列表：
  /var/lib：20.00 GiB
  /var/log：8.00 GiB
说明：按实际分配块统计；不代表可以安全删除。

[完成] 系统日志
检查范围：当前身份可见的活动及归档日志，不是 /var 的独立占用
日志占用：256 MiB（上游格式化值换算，近似）
说明：仅查询占用，不读取日志正文；可能已包含在大目录中。

[部分完成] Docker
分类：镜像 12.0 GB，本地卷 8.0 GB，容器可写层 500 MB，构建缓存 1.0 GB
说明：Docker 展示值为近似；某些容器未发现支持的日志文件，未将其按零处理。

[权限不足] 已删除仍占用
说明：当前用户无法看到部分进程，不能据此判断整个服务器没有该类占用。

各项范围可能重叠，请勿相加。分享前检查路径、进程名和容器标识是否包含敏感信息。
