# 「服务器空间去哪了」开发计划

## 已确认目标

开发在 Linux 服务器上运行的中文命令行只读排查工具，说明空间主要被什么占用、哪些检查没有完成。全部新产物保存在本目录，不向原知识库写入、不提交申请、不连接现有服务器。2026-09-03 已授权将本项目源码公开到 GitHub 并持续维护；本机测试环境、凭据和真实报告继续保留在本地，不公开。

## 第一版范围

- 分区总容量、已用、可用空间和文件数量额度。
- `--path` 指定目录，默认 `/`；`--deep` 主动开启大目录遍历，显示前 20 项。
- 系统日志、普通本机 Docker 分类与支持的容器日志占用。
- Linux 文件描述符持有的已删除文件，按设备号和文件编号去重；不承诺覆盖仅由内存映射持有的文件。
- `--timeout` 设置单项检查上限，默认 30 秒；超时和取消不伪装成完整结果。
- 默认仅终端输出；`--output` 指定目录后保存中文文本和结构化报告，不覆盖已有报告。

## 实现与安全

Python 3.10 及以上、标准库和固定参数的系统工具。结构分为采集、整理、中文呈现和报告保存。未知数据不等于零；状态包括完成、部分完成、权限不足、工具缺失、超时、失败以及未启用/不适用。不同范围的数字不相加，不推算安全可删除总量。

不删除、清空、重启、自动提权、安装运行依赖、上传信息，不读取日志正文、进程环境变量或容器密钥。目录不跟随符号链接、不跨挂载点。只读指不主动修改被检查文件、配置和容器状态，不承诺操作系统完全不产生访问记录。

## 开发和验收阶段

1. 先写失败测试，再实现中文命令、分区、目录、超时取消、保存报告。
2. 添加日志、Docker、已删除占用，验证缺少工具、权限不足、进程退出、文件消失等情况。
3. 本机专用 Linux 虚拟机：Ubuntu 24.04、Debian 12 依次验证，每套默认 2 核、2 GB 内存、16 GB 虚拟磁盘。Lima 与镜像、实例状态均在本目录，不挂载个人目录。空间与文件数量耗尽测试只在虚拟机内的小型专用分区进行。
4. 输出源码包、单文件运行包、中文说明、合成示例、测试报告、已知限制、更新记录和 MIT 许可证。只有实际测试的平台可以标为已验证。

测试覆盖普通文件、特殊文件名、稀疏文件、硬链接、重复文件描述符、挂载边界、日志轮转、不同日志驱动、管理员和普通用户权限、超时、取消和保存失败。报告记录实际版本、通过项和失败项。

## 文件组织与交付

代码、文档、测试、测试环境、报告、发布包按用途分目录。虚拟机、缓存、真实机器报告和凭据不得进入公开发布包。开发按约 7–10 个全职工作日的原估计组织，但完成以实际验收为准，不以时间或模拟测试代替 Linux 实测。

最终标准：使用者按说明能在已验证的 Linux 环境执行工具，得到可信且能看懂的报告。公开试用、真实维护记录和资格申请属于后续步骤，项目完成不保证获得资格。

## 实施中的安全调整

- 容器日志不使用 `docker inspect`：该命令即使只显示指定字段，底层仍会取得完整容器配置。改为本机数据目录与合法容器编号推导标准日志路径，只读取文件元数据。
- 未识别的日志存储、检查尚未开始即超时、完全无权限等情况，大小字段使用空值或省略，不显示虚假的零。
- Docker 日志目录逐层禁止符号链接；普通目录遍历打开后核对设备、文件编号和挂载编号，以识别检查期间的路径变化。

## 0.1.0 历史实施进展：2026-09-03

- 阶段一、二：已实现全部首版检查与中文报告；默认不保存、不清理，错误和未完成状态独立呈现。
- 阶段三：Ubuntu 24.04.4、Debian 12.15 的 ARM64 专用虚拟机均通过 83 项自动测试及 9 组真实场景、66 条断言。真实失败、修复和测试样本调整保留原始记录，详见 `报告/验收报告.md`。
- 阶段四：源码、单文件运行包、中文说明、合成示例、许可证与验证记录齐备，发布物只包含白名单文件。未验证的架构和运行时明确列在说明中。
- 交付仅限本地试用与公开发布准备；未上传仓库，未提交申请，不承诺资格审核结果。

## 0.1.1 扩大验收：2026-09-03

- 根据追加要求，新增边界及安全回归测试，并修复扩大检查实际发现的问题。没有把旧版通过记录当作新版验证。
- Ubuntu ARM/Python 3.12、Debian ARM/Python 3.11、Debian ARM/Python 3.10、完整 Debian x86_64 软件模拟/Python 3.11，四个组合各通过 147 项自动测试和 19 组真实场景、133 条断言，无跳过或失败。
- 新增真实非空构建缓存、共享镜像层、压缩轮转、多日志驱动、跨进程文件去重、并发对象消失、工具缺失与服务停止、原始字节文件名等场景。
- 原生 x86_64 硬件、生产大规模长期负载仍未验证。故障、验收脚本错误及环境安装失败记录保留，详细证据见 `报告/扩大验收记录.md` 与 `报告/需求覆盖核对.md`。
- 正式包已与实测候选逐成员核对，源码解压后重复完整自动测试，并在 Linux 对两个正式入口完成实际保存、不覆盖等 16 条断言。发布在本地 `发布包/0.1.1`，旧版保留历史，不覆盖原文件。
- 最终 Ubuntu、Debian 虚拟机均已停止；包含嵌套 x86_64 的测试环境保留约 7.77 GiB，无运行中的测试虚拟机。首版本地交付完成，未上传仓库或提交申请。
