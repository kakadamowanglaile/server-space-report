# 更新记录

## 0.1.2 — 真实资源耗尽与连续取消修正（验证中）

- 新增真实文件描述符额度耗尽回归，检查管道创建失败能否返回明确失败状态。
- 新增连续两次取消的真实辅助进程回归，验证清理不会被第二次信号中断。
- 选定目录在检查途中被重命名或替换时，不再扫描另一个目录并显示完整；采集使用已打开的原目录，路径变化会明确提示。
- 修正同进程重复扫描时递归闭包保留文件索引的问题；索引及时释放，持续测试同时检查内存增长。
- 追加十万真实文件、同进程重复扫描及 Ubuntu/Debian 各十分钟持续并发负载；结果另见本地持续负载验收记录。完成前不把旧版本的测试结果当作新版通过。

## 0.1.1 — 扩大测试与安全修正

- 修正动态硬链接、挂载路径控制字符、文件描述符复用和权限变化时的统计。
- 部分日志读取失败后保留已取得的结果；异常元数据不再中断整项报告或冒充零。
- 超时覆盖完整结果传输；辅助命令退出后清理其同组后代；管道接收端关闭时正常结束输出。
- 报告保存改用目录描述符，不跟随输出路径的符号链接，目录替换和持久化失败不会显示成功。
- 运行包与源码包共用源文件白名单，增强源文件与输出目录的路径替换防护。
- 新增实际运行环境和拓展测试；最终通过范围以扩大验收记录为准，旧版本仅保留作历史证据。

## 0.1.0 — 首个本地试用版

- 中文命令行报告，分区、目录、系统日志、Docker 和已删除仍占用文件检查。
- 独立限时、部分结果状态、终端控制字符转义和不覆盖的报告保存。
- 无第三方 Python 运行依赖；源码与单文件发布包。
- 验证结果和限制另见测试报告，不把单元测试等同于真实 Linux 验收。
