# 更新记录

## 未发布 — CI 维护（2026-09-05）

- GitHub Actions 的 Python 环境配置由 `actions/setup-python@v5` 更新至 `@v7`，移除 Node.js 20 弃用提示；测试矩阵及项目运行行为不变。

## 未发布 — 交付核对工具维护（2026-09-04）

- 移除过时的 150 项测试默认值；交付核对必须显式传入候选验收记录中的预期测试数量，缺少参数时在读取归档前提示并退出。
- 源码包包含交付核对工具，中英文贡献指南说明调用参数、测试数量与候选验收记录的关系。
- 增加遗漏预期数量的回归检查，并验证核对工具确实进入源码包。扫描程序和 JSON 格式未改变，下列 0.1.2 验收数量仍为当时的历史记录。
- 交付核对会自动创建缺失的临时目录，并兼容 `unittest` 单个测试的 `Ran 1 test` 输出；新克隆也能完成核对。
- 将交付核对的安全与一致性检查改为显式校验，`python -O` 也不会跳过；增加优化模式回归检查。

## 0.1.2 — 资源、取消、目录变化与重复扫描修正（2026-09-03）

- 新增真实文件描述符额度耗尽回归，检查管道创建失败能否返回明确失败状态。
- 新增连续两次取消的真实辅助进程回归，验证清理不会被第二次信号中断。
- 选定目录在检查途中被重命名或替换时，不再扫描另一个目录并显示完整；采集使用已打开的原目录，路径变化会明确提示。
- 修正同进程重复扫描时递归闭包保留文件索引的问题；索引及时释放，持续测试同时检查内存增长。
- Ubuntu 与 Debian 各通过 154 项自动测试、21 组真实场景的 161 条断言，以及十万文件下的完整十分钟持续负载；两套合计 663 次 CLI 调用。Debian Python 3.10 另通过 154 项自动测试。
- 保留首次 Debian 日志轮转测试预期失败的记录，明确区分未知大小与已知零值，加入停止轮转后的原生 stat 对照后完整复测通过。
- README 与贡献指南提供中英文版本，源码包包含四份双语文档；命令行帮助、终端输出和 JSON 字段仍为中文。
- 更新公开验收说明，区分已测环境、历史结果与长期生产等未验证范围。

## 0.1.1 — 扩大测试与安全修正

- 修正动态硬链接、挂载路径控制字符、文件描述符复用和权限变化时的统计。
- 部分日志读取失败后保留已取得的结果；异常元数据不再中断整项报告或冒充零。
- 超时覆盖完整结果传输；辅助命令退出后清理其同组后代；管道接收端关闭时正常结束输出。
- 报告保存改用目录描述符，不跟随输出路径的符号链接，目录替换和持久化失败不会显示成功。
- 运行包与源码包共用源文件白名单，增强源文件与输出目录的路径替换防护。
- 新增实际运行环境和拓展测试；最终通过范围以扩大验收记录为准，旧版本仅保留作历史证据。

## 0.1.0 — 首个本地试用版

- 中文命令行报告，分区、目录、系统日志、Docker 和已删除仍占用文件检查。
- 独立限时、部分结果状态、终端控制字符转义和不覆盖的报告保存。
- 无第三方 Python 运行依赖；源码与单文件发布包。
- 验证结果和限制另见测试报告，不把单元测试等同于真实 Linux 验收。
