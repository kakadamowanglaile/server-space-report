# 服务器空间去哪了

[English](README.md) | **简体中文**

0.1.2 已完成下述 Linux 验收，可在已验证的环境中使用。已修正低文件描述符额度、连续两次取消、检查途中路径被替换及重复扫描内存增长的问题；验证范围和仍未验证的限制见下文。

文档提供中英文版本。当前命令行帮助、终端输出和 JSON 字段仍为中文，尚无 `--lang` 参数。

一次运行的 Linux 本机只读空间报告。查看分区、大目录、系统日志、Docker 分类及已删除但仍被进程打开的文件。

它不删除文件、不清空日志、不重启服务、不自动提权、不上传信息。无需模型接口、账号或第三方 Python 包。默认只显示结果，不保存文件。

## 使用

需要 Python 3.10 或更新版本。源码包解压后，在项目目录执行：

```sh
python3 -B 代码/空间去哪了.py
python3 -B 代码/空间去哪了.py --path /var --deep
python3 -B 代码/空间去哪了.py --path / --timeout 30 --output ./排查报告
```

单文件包同样需要 Python，下载到本地后执行：

```sh
python3 服务器空间去哪了-0.1.2.pyz --help
python3 服务器空间去哪了-0.1.2.pyz --path /var --deep --output ./排查报告
```

程序不会自动安装缺失依赖。系统日志检查需要 `journalctl`，Docker 检查需要本机 Docker CLI 和 `/var/run/docker.sock`；缺失会明确显示，其他检查仍继续。普通用户可用，但可能看不到系统日志、容器数据和其他用户的进程。管理员权限由使用者自行决定，程序不会请求密码或自动提权。

| 参数 | 用途 |
|---|---|
| 无参数 | 基础检查，不遍历目录树 |
| `--path 目录` | 检查该目录所在分区，默认 `/`；路径不能经过符号链接 |
| `--deep` | 主动遍历目标目录，显示前 20 个大目录；不跨子挂载点，不跟随符号链接 |
| `--timeout 秒数` | 单项检查上限，默认 30 秒；返回部分结果可能额外需要短暂交接时间 |
| `--output 目录` | 创建独立时间戳子目录，保存 `报告.txt` 与 `报告.json`，不覆盖旧报告；路径不可经过符号链接 |
| `--version` | 版本号 |
| `Ctrl+C` | 取消后续检查；已显示结果保留，指定输出目录时保存部分报告 |

退出码：`0` 表示所有已执行检查完成；`1` 表示至少一项未完成、权限不足或超时，应查看分项说明；`2` 表示参数、运行系统或报告保存失败；`130` 表示用户取消。普通用户运行时因部分数据不可见而返回 `1`，仍可阅读其他已完成结果。

## 怎么看结果

- **分区空间**是整个文件系统的用量；指定 `/var` 不会把它变成 `/var` 独占的分区。
- **大目录**只在 `--deep` 开启后采集，按实际分配块统计。硬链接只计一次，因此跨目录硬链接的占用归属受遍历顺序影响。
- **系统日志**是当前身份可见的活动及归档日志汇总，不读取正文，也不能按指定目录精确分摊。
- **Docker**分类是该本地守护进程的全局量。Docker 展示大小经过格式化，换算字节属于估算；其“可回收”不等于业务上可删。
- **已删除仍占用**按文件系统查找文件描述符持有的已删除普通文件。多个进程打开同一个文件只计一次，不覆盖仅由内存映射持有的情况。

这些数字可能包含相同内容，不要相加，也不要据此直接删除数据。权限不足、工具缺失、超时和失败都不等于占用为零。

报告可能包含路径、进程名和容器标识，分享前请检查。只读指不主动修改被检查对象；目录读取可能影响访问时间，系统可能记录审计日志。目录扫描会产生读取负载，超大目录可缩小范围检查。

## 支持与边界

2026-09-03，**0.1.2** 完成以下专用 Linux 环境验收，表中 Linux 自动测试均无失败、无跳过。

| 环境 | 自动测试 | 真实功能场景 | 持续负载 |
|---|---|---|---|
| Ubuntu 24.04.4 ARM64 / Python 3.12.3 / Docker 29.1.3 | 154 项通过 | 21 组、161 条断言通过 | 600.553 秒、339 次 CLI 调用 |
| Debian 12.15 ARM64 / Python 3.11.2 / Docker 20.10.24 | 154 项通过 | 21 组、161 条断言通过 | 600.372 秒、324 次 CLI 调用 |
| Debian 12.15 ARM64 / Python 3.10.21 | 154 项通过 | 本版未用该解释器重复执行 | 本版未用该解释器重复执行 |

两套持续测试均使用十万个真实文件、稀疏文件和硬链接，分配字节与 GNU `du` 对照一致；源码和单文件包均实际执行扫描、报告保存、超时、取消及连续两次取消。两套完成的测试合计 663 次 CLI 调用、110,734 次合成文件变动，样本和源码指纹未变，没有残留检查子进程或清理错误。

同进程连续扫描 12 次后，文件描述符均保持 4 个，RSS 峰值首尾分别保持在 Ubuntu 33,468 KiB、Debian 32,136 KiB。持续调用的进程树 RSS 采样峰值分别为 56.19 MiB、66.63 MiB；采样合计不是精确独占内存。

Debian 的首次负载因一项日志轮转测试预期失败：程序明确返回部分完成且未伪造零值，脚本却要求每次都取得元数据。修正后仍要求变化时明确未知、本次受控负载至少 95% 的完整调用取得元数据，并在停止轮转后与原生 `stat` 精确对照。完整复测通过，260 次完整调用均取得元数据。首次失败保留在本地，不计入上述完成次数。修复与验证边界详见 [验收说明](文档/验收说明.md)。

本版 macOS 开发检查为 154 项中 147 通过、7 项 Linux 专用测试跳过；macOS 不支持功能扫描。0.1.1 曾在完整 x86_64 软件模拟 Debian 中通过旧版验收，不能当作 0.1.2 全部场景重新验证。GitHub Actions 的单元与接口测试与虚拟机真实场景分开记录。

十分钟测试不能证明长期生产稳定性。真实生产数据、长期大规模负载、原生 x86_64 的 Docker/挂载/负载场景、其他系统及 Docker 版本、外部用户采用仍未验证。Windows、远程 Docker、无根 Docker、容器内替代主机检查、快照与存储池对账不在首版范围。

更多边界见 [已知限制](文档/已知限制.md)，结构化字段见 [报告格式](文档/报告格式.md)，文字样式见 [人工构造的示例](文档/报告示例.txt)。

## 参与维护与反馈

问题反馈请使用 [GitHub Issues](https://github.com/kakadamowanglaile/server-space-report/issues)，说明系统、Python 版本、执行命令、预期与实际行为，只提供已脱敏的必要信息。

开发环境、测试命令、外部贡献者的 Fork 流程和上传范围见 [贡献指南](CONTRIBUTING.zh-CN.md)。修改文档时请同步中英文版本。

正式发布物以 [Releases](https://github.com/kakadamowanglaile/server-space-report/releases) 和对应验证说明为准。源码公开不代表开发版本已完成验收。

## 许可证

项目使用 [MIT 许可证](LICENSE)。源码包按明确白名单构建，不包含虚拟机、测试密钥、下载缓存或真实机器报告。
